"""Outbound calling for RupeeGPT (Day 6).

- ``agent.py`` — the outbound voice agent worker.
- ``dial.py`` — dispatches a call (run from a second terminal).
- ``reminder.py`` — the Day 6 demonstration reminder (scheme + deadline).
"""
